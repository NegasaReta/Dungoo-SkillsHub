VITE_API_BASE_URL=http://localhost:8000

# Runs the app against an in-browser mock of the API so the frontend works with
# no backend. Set to false once the FastAPI service is available.
VITE_USE_MOCK_API=true
